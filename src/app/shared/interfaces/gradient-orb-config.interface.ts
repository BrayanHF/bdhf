export type GradientOrbPosition = 'left' | 'center' | 'right';

export interface GradientOrbConfig {
  top: string;
  left: string;
  size: number;
  color: string;
  opacity: number;
  blur: number;
}
